<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Save Confirmation</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Flights Saved</h1>

    <% if (locals.error) { %>
        <p class="error-message"><%= locals.errorMessage || ("Failed to save flights for " + user + ".") %></p>
    <% } else { %>
        <p>Added <strong><%= count %></strong> flight(s) for user <strong><%= user %></strong> (<%= total %> total saved).</p>
    <% } %>

    <hr>
    <a href="/">HOME</a>
</body>
</html>
